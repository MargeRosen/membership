<h2>Add Band Membership</h2>
<%= form_for(@band_membership) do |f| %>
  <% if @band_membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@band_membership.errors.count, "error") %> prohibited this band from being saved:</h2>

      <ul>
      <% @band_membership.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start %><br>
    <%= f.date_field :start %>
  </div>
  <div class="field">
    <%= f.label :left %><br>
    <%= f.date_field :left %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

